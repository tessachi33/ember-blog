<!-- {{#if isEditing}}
  {{partial 'post/edit'}}
  <button {{action 'doneEditing'}}>Done</button>
{{else}}
  <button {{action 'edit'}}>Edit</button>
{{/if}} -->

<h1>{{title}}</h1>
<h2>{{email}}</h2>

<hr>

<div class="body">
  {{format-markdown body}}
</div>
